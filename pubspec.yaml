name: mvl_app_core
description: "An App Core features for every app"
version: 1.0.0
homepage: https://github.com/ModelViewLabs/app_core.git

publish_to: none

module:
  androidX: true

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  version: any
  uuid: any
  url_strategy: any
  path: any
  logging: any
  async: any
  badges: any
  clock: any
  collection: any

flutter:
